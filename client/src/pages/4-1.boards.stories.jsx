import React from 'react';
import Boards from './4-1.boards';

export default {
  title: '4-1.boards',
  component: Boards,
};

const Template = (args) => <Boards />;

export const FirstStory = Template.bind({});

// FirstStory.args = {
//   /*👇 The args you need here will depend on your component */
// };